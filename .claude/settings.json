{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -Command \"if ($env:CLAUDE_FILE_PATH -match '\\.(env|env\\.local|env\\.production)$') { Write-Error 'BLOCK: Cannot edit environment files containing secrets'; exit 1 } else { exit 0 }\""
          }
        ]
      }
    ]
  }
}
